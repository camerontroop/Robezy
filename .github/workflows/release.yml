name: Release Build

on:
  workflow_dispatch: # Allows manual trigger button
  push:
    tags:
      - 'v*' # Trigger on version tags (e.g., v1.0.0)

jobs:
  build-and-release:
    permissions:
      contents: write
    
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, windows-latest]

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Dependencies (Linux/Mac)
        if: matrix.platform == 'ubuntu-latest' || matrix.platform == 'macos-latest'
        run: |
          npm install

      - name: Install Dependencies (Windows)
        if: matrix.platform == 'windows-latest'
        run: |
          npm install

      - name: Build App
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__ # the action automatically replaces this with the version
          releaseName: "RoBezy v__VERSION__"
          releaseBody: "See the assets to download this version and install."
          releaseDraft: true
          prerelease: false

      - name: Zip macOS App (Fix for Damaged Error)
        if: matrix.platform == 'macos-latest'
        run: |
          cd src-tauri/target/release/bundle/macos
          zip -r RoBezy_Mac_App.zip RoBezy.app
          
      - name: Upload macOS Zip
        if: matrix.platform == 'macos-latest'
        uses: softprops/action-gh-release@v1
        with:
          files: src-tauri/target/release/bundle/macos/RoBezy_Mac_App.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
